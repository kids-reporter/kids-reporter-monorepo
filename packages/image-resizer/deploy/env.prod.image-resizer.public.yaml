# Public (non-secret) runtime env vars for the prod-image-resizer Cloud Run
# service (Eventarc-triggered on GCS object-create, not directly by users).
# Passwords must never be added here — see deploy/secrets/image-resizer.secrets.

TARGET_FOLDER: 'resized'
TARGET_SIZES: '2000,1200,800,400'
PROJECT_ID: 'kids-reporter'
