# Public (non-secret) runtime env vars for the staging-cms Cloud Run service
# (full Keystone CMS + Admin UI, Dockerfile, runs DB migrations via run.sh).
# Passwords, connection strings, API keys, and other secrets must never be
# added here — see deploy/secrets/cms.secrets instead.

IS_UI_DISABLED: "false"
DATABASE_PROVIDER: "postgresql"
SESSION_MAX_AGE: "86400"
FILES_BASE_URL: "/files"
FILES_STORAGE_PATH: "public/files"
IMAGES_BASE_URL: "/images"
IMAGES_STORAGE_PATH: "public/images"
MEMORY_CACHE_TTL: "300000"
MEMORY_CACHE_SIZE: "300"
TWO_FACTOR_AUTH_COOKIE_NAME: "keystonejs-2fa"
TWO_FACTOR_AUTH_SERVICE_NAME: "KidsReporter Keystone"
TWREPORTER_ID: "013371828254368986439:_ega685nikw"
GO_API_JWT_ISSUER: "https://staging-go-api.twreporter.org"
GO_API_JWT_AUDIENCE: "https://staging.twreporter.org"
PREVIEW_SERVER_PATH: "/preview-server"
NODE_ENV: "production"
CORS_ALLOW_ORIGINS: "http://localhost:3000,https://staging-kids.twreporter.org"
TWO_FACTOR_AUTH_ENABLE: "true"
GCS_ORIGIN: "https://staging-kids-storage.twreporter.org"
PREVIEW_SERVER_ORIGIN: "https://staging-frontend-for-preview-ehh4zj53ca-de.a.run.app"
