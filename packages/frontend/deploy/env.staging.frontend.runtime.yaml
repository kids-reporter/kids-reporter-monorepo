# Server-only (non-NEXT_PUBLIC_) runtime env vars for the staging-frontend
# Cloud Run service, read by src/environment-variables.ts for server-side
# (SSR/RSC) calls to internal Cloud Run services. Passwords, connection
# strings, API keys, and other secrets must never be added here — see
# deploy/secrets/frontend.secrets instead.

INTERNAL_API_GATEWAY_ENDPOINT: "https://staging-api-gateway-ehh4zj53ca-de.a.run.app"
INTERNAL_CONTENT_API_ENDPOINT: "https://staging-content-api-ehh4zj53ca-de.a.run.app"
INTERNAL_GQL_ENDPOINT: "https://staging-api-gateway-ehh4zj53ca-de.a.run.app/api/graphql"
