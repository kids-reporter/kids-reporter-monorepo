# Public (non-secret) runtime env vars for the prod-cronjob-scheduled-post
# Cloud Run Job. Passwords must never be added here — see
# deploy/secrets/scheduled-post.secrets instead.

API_URL: "https://prod-cms-496849015885.asia-east1.run.app/api/graphql"
